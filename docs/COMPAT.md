# Compatibility matrix

The skills in this suite encode workarounds for concrete tool limitations. Those workarounds are
only correct against the versions they were verified on — and prose buried in a runbook gives no
way to tell what is still true. This table is the single place that records **what was checked,
when, and what would make it stale**.

Update a row when you verify it; add the date. A stale row is a bug report waiting to happen.

| Tool | Verified version | Date | Constraint encoded in the suite | Retest when |
| --- | --- | --- | --- | --- |
| Orca CLI / app | `1.4.184` | 2026-08-20 | The command surface is loaded from the binary at run time (`orca skills get orchestration`, `orca skills get orca-cli`) and never copied into this repo. | Never pinned on purpose — but if a skill starts naming a flag that `orca <cmd> --help` does not list, that flag leaked out of the binary and into prose. |
| Orca `check --json` | `1.4.185` | 2026-08-21 | Keepalives emit JSON to **stderr** every 15s (`_keepalive`; `_heartbeat` is a deprecated alias). Concatenated JSON objects need a decoder loop (`raw_decode`), not a single `json.load`. | A CLI change to `check`. |
| `gh` | `2.83.1` (2025-11-13) | 2026-08-20 | `gh issue create` has **no `--depends-on`** flag and the REST dependencies endpoint 404s, so `/orca-tasks` carries dependency edges in the Orca DAG (`--deps`) and documents them in the issue body. `gh issue create` also has **no `--json`** — the issue number is parsed from the URL on stdout. | A `gh` upgrade. Re-probe with `gh issue create --help \| grep depends-on`; if it appears, the real blocking link becomes available and the body mirror becomes redundant. |
| `gh` (checks) | `2.83.1` | 2026-08-20 | `gh pr checks` on a repo with **no** workflows prints `no checks reported` and **exits 0**. An empty check set is therefore indistinguishable from a green one by exit code alone — which is why the merge gate is a capability recorded at setup, not something inferred at merge time. See [`skills/orca-setup/SKILL.md`](../skills/orca-setup/SKILL.md). | A `gh` upgrade changing the exit code, or GitHub changing the empty-checks response. |
| opencode | `1.18.15` | 2026-08-20 | `-a <agent>` is **broken in TUI mode** (prints help and exits), so the `worker` profile is selected with `OPENCODE_CONFIG_CONTENT='{"default_agent":"worker"}'` plus `--auto`. Also defaults to prompting for `external_directory` and `doom_loop`, which the worker profile auto-allows. | An opencode upgrade. Re-probe with `opencode -a worker` in a TUI terminal; if it launches instead of printing help, the env-var workaround can be dropped from `/orca-orchestrate`. |
| Claude Code | `2.1.227` | 2026-08-20 | Agent pairs install to `~/.claude/agents/`; the permissive worker uses `permissionMode: bypassPermissions`. A pre-existing root-owned `~/.claude/agents/` blocks the copy. | A change to the agent frontmatter schema or the agents directory location. |
| Grok CLI | `1.0.5` | 2026-08-21 | Agent profiles load from `~/.grok/agents/` (user) and `.grok/agents/` (project); `--agent <name>` selects the session profile. `permission_mode` accepts `bypassPermissions` (same name as Claude Code), and `--always-approve` is the CLI-side safety net. Grok also discovers `~/.claude/agents/` — but as **subagents**, not as the session profile. `-p` / `--prompt-file` is real headless (`grok --agent worker -p "..."` runs the composed worker profile); it is **not** the supervised TUI recipe and is **not** what `worker-read` returns. That recipe (`terminal create --command "grok --agent worker --always-approve"` then `worker-start --terminal`) is an `external_terminal`: `worker-read --source auto` returns `source: "terminal"` with `fallbackReason: session_not_reported` (Orca `1.4.185` orchestration guide: hook-reported Codex/Claude/OpenClaude/Grok transcripts only when Orca can prove the worker session). Evidence: candigo `run_73627a191505`. `tui-idle` is not grok-ready; availability is `orca terminal read` until the status bar shows `always-approve` and the prompt is `❯`. | A Grok upgrade changing the agent frontmatter schema, the `permission_mode` values, or the agents directory. An Orca upgrade that proves Grok sessions for `worker-read` on `external_terminal` (the fallback becomes a bug). `tui-idle` becoming grok-ready. |
| Grok CLI (folder trust) | `1.0.5` | 2026-08-21 | First launch in a git repo shows `Do you trust the contents of this directory?` and waits forever. `--always-approve` does not cover it. Trust is per git repo (later worktrees of the same repo skip the invite). Persist with `--trust` (writes `~/.grok/trusted_folders.toml`). `grok --help` omits `--trust`; zsh completions list `--trust[Trust this folder and persist the decision to the trust store]` and the hooks doc names launching with `--trust`. This binary accepts it: `grok --trust --version` exits 0 and `grok --trust -p "..."` runs; `grok --not-a-real-flag` errors `unexpected argument`. | A Grok upgrade that lists `--trust` in `--help`, rejects it, or changes the invite/store. Re-probe `grok --help` and `grok --trust --version`. |
| Hermes Agent | `0.20.5` (2026.8.19) | 2026-08-22 | **No per-session agent directory and no `--agent` flag** (`--profile/-p` exists but is hidden from `--help`; it selects a whole config+skills home, not a session persona). The pair installs two ways: as skills `orca-worker` / `orca-orchestrator` in `~/.hermes/skills/`, selected by preloading (`hermes chat --skills orca-worker --yolo` — `--yolo` skips dangerous-command approvals, the opencode `--auto` equivalent), **and** as real Hermes profiles of the same names (`hermes profile create … --clone` + wrapper aliases in `~/.local/bin/`), which carry the role permanently in their `SOUL.md` and ship `approvals.mode: off`. Skills symlinked into `~/.hermes/skills/` resolve normally; `AGENTS.md` in the cwd is auto-loaded; `chat -q` is headless. **Run-measured (Orca probe, 2026-08-22):** full cycle dispatch → injected preamble read → task executed → self-sent `worker_done succeeded` in 1m44s; a fresh profile's cold start is 2-4 min (tirith install + MCP servers) before any TUI paint; `tui-idle` fired `satisfied: true` while the screen still showed the bare shell — bind only after `terminal read` shows the banner line `Profile: <name>` and the `❯` prompt (same trap as grok). | A Hermes upgrade adding a per-session agent directory or an `--agent` flag; a rename of `--skills`/`--yolo`/profile wrapper mechanics. Re-probe with `hermes --help` and `hermes profile create --help`. Cold-start time on a second machine, or a Hermes build that paints its TUI faster. |
| freebuff | `0.0.150` | 2026-08-20 | **No headless mode** — the CLI accepts only `login`, `--continue`, `--cwd`, `-v`, `-h`; every scripting flag (`--print`, `-p`, `--json`, `--non-interactive`, `--batch`, `--exec`, `--auto`, `--script`, `--prompt`) is rejected as `unknown option`. `@codebuff/sdk` needs a paid key. Hence the terminal-driven TUI pattern in [`skills/orca-freebuff/SKILL.md`](../skills/orca-freebuff/SKILL.md): marker polling + impersonated `worker_done`. | A freebuff upgrade. Re-probe `freebuff --help`; if any scripting flag appears, the TUI-driving pattern collapses into a normal dispatch and the skill should be rewritten, not patched. |
| freebuff (concurrency) | `0.0.150` | 2026-08-20 | **One instance per machine.** A second launch is refused with `Freebuff is already running / Only one freebuff instance is allowed at a time`, offering *Take over* (which kills the first) or *Exit*. Freebuff tasks are therefore strictly serial. The lock `~/.config/manicode/freebuff-instance-owner.json` (`instanceId` + `pid`) **is not removed on exit**, so a dead pid remains — probe with `pgrep -fl freebuff`, not the file. | A freebuff upgrade adding multi-instance support. If a second instance ever starts cleanly, the serialization rule in `/orca-freebuff` and `runtimes.md` can be dropped. |
| freebuff (quota) | `0.0.150` | 2026-08-20 | **The session is a one-hour wall-clock window, not a task counter.** Measured: banner `DeepSeek V4 Flash 07/31 · 42m left` decayed to `41m` after a 20-second task and to `37m` while idle. A new terminal opened inside the window **joins** the running session rather than starting one. `N of N premium sessions used · resets in <h>` is a separate premium-tier counter and does not move on the free Flash tier. Region matters: supported regions get full access, unsupported regions and detected VPNs fall back to a capped tier with lighter models. | A change to the banner format, or a move between regions/tiers. The banner is the only quota surface — the CLI has no usage/status command. |

## How the suite is meant to degrade

Every workaround above is a *fallback from a missing capability*. When a tool gains the
capability, the correct move is to delete the workaround, not to keep both paths — two paths mean
two behaviours to reason about and only one of them gets tested.
